node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
!.env.production.example

# Investor demo capture rig + rendered videos (large; lives here but is a
# separate concern from the dashboard app — never ship it in the deploy).
demo/
